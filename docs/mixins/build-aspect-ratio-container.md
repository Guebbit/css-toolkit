# @mixin build-aspect-ratio-container

Generates only the container part of the aspect-ratio pattern.

## Usage

```css
.ratio-box {
  @mixin build-aspect-ratio-container 56%;
}
```

Use with `build-aspect-ratio-anchor` on the child you want stretched.
