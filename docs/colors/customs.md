# Custom colors

Custom palettes exported from `src/colors/customs.js`.

Current families:

- `flatricaGreen` (+ `flatricaGreen100..900`)
- `mangaRed` (+ `mangaRed100..900`)

Each family has computed tint/shade variants, useful for utility generation and theming.
